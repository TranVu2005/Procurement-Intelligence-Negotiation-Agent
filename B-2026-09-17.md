# PHẢN HỒI CỦA B CHO A VÀ C – 2026-09-17

Mình đã đối chiếu các branch với phản hồi mới nhất của C.

## 1. Trạng thái hiện tại

- Phần Reasoning/Planning của B đã hoàn thành và đã được tích hợp vào graph.
- Các bài test riêng của B và graph hiện tại: **41/41 test pass**.
- C đã:
  - sửa `RECURSION_LIMIT` thành 40;
  - bổ sung `replan_reason` vào `AgentState`;
  - bổ sung JSON response cho `StubLLM.bind()`;
  - thêm các test liên quan.
- A đã đưa phần xử lý perception thật vào `src/nodes/perceive.py`.
- Khi chạy toàn bộ test trên phiên bản mới nhất: **241 test, còn 2 test lỗi**.

## 2. Lỗi đang chặn việc merge vào main

Hai test lỗi đều xuất phát từ `src/perception/parser.py`.

Hiện file này vẫn tự khởi tạo `ChatGoogleGenerativeAI` với model `gemini-2.5-flash`, nên khi chạy chế độ:

```powershell
$env:AGENT_LLM="stub"
```

parser vẫn yêu cầu `GOOGLE_API_KEY` và không sử dụng `StubLLM` chung của project.

Trong khi đó, phần `StubLLM.bind()` mà C vừa bổ sung đã hoạt động đúng. Vì vậy chưa cần sửa phần của C; cần cho parser sử dụng chung LLM factory của hệ thống.

## 3. Việc cần A xử lý

Nhờ A sửa `src/perception/parser.py`:

- bỏ LLM factory riêng trong parser;
- sử dụng `get_llm()` từ `src/llm.py`;
- vẫn giữ `.bind(response_format=...)` để nhận structured JSON;
- kiểm tra lại mapping:
  - `supplier_ids` → `target_supplier_ids`;
  - các trường perception theo đúng `AgentState` và interface contract.

Sau khi sửa, A vui lòng push commit lên branch `hien` hoặc gửi commit hash để nhóm lấy về kiểm tra.

## 4. Việc cần C xử lý sau khi nhận bản sửa của A

Nhờ C:

1. Merge/cherry-pick commit sửa parser của A vào `role-c-implementation`.
2. Chạy lại toàn bộ test với `AGENT_LLM=stub`.
3. Chạy AutoEval và smoke test đủ 4 intent:
   - `search`
   - `compare`
   - `detail`
   - `negotiate`
4. Nếu tất cả đều pass thì báo lại kết quả và commit hash để merge vào `main`.

## 5. Những phần tiếp theo sau khi test xanh

Sau khi xử lý blocker trên, nhóm vẫn cần hoàn thiện:

- A: xác định và triển khai cơ chế memory/history.
- C: nối memory vào LangGraph.
- A/C: bổ sung dữ liệu nguồn thật, đặc biệt `nguon_url`, vì dữ liệu hiện tại còn nhiều giá trị null.
- C: chạy thử end-to-end với dữ liệu thật.
- Cả nhóm: cập nhật interface contract nếu còn thiếu `intent`, `supplier_id` và `supplier_ids`.

Phần verifier của B đang cố ý chặn kết quả thiếu nguồn. Không nên nới lỏng verifier chỉ để pipeline chạy qua; cần bổ sung dữ liệu nguồn hợp lệ.

## 6. Kết luận

Hiện tại **chưa nên merge toàn bộ `role-c-implementation` vào `main`**, vì full test vẫn còn 2 lỗi. Blocker trước mắt là `src/perception/parser.py` chưa sử dụng LLM factory chung.

B sẽ tiếp tục kiểm tra lại toàn bộ test và luồng Reasoning/Planning ngay khi A gửi commit sửa parser.
